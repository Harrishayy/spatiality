# Wheels installed offline on Kaggle. Verified 2026-04-25: Kaggle is Python 3.12 / torch 2.10 / CUDA 12.8.
# DO NOT include torch / torchvision / numpy / opencv-python / pillow — preinstalled on Kaggle.
#
# Pins are floors only — pip download with --platform/--abi will pull the latest cp312 wheel >= floor.
# This protects against PyPI dropping older wheels for a newer Python (which is what bit safetensors==0.4.5).

# VGGT runtime deps (model is loaded from local weights or HF cache)
einops>=0.8
huggingface-hub>=0.27
safetensors>=0.4.5
imageio>=2.35
imageio-ffmpeg>=0.5

# 3DGRUT runtime / build helpers
ninja>=1.11
plyfile>=1.1
trimesh>=4.4
scikit-image>=0.24
pyyaml>=6
hydra-core>=1.3
omegaconf>=2.3

# Pipeline plumbing
pydantic>=2.7
pydantic-core>=2.18
typing-extensions>=4.12

# Observability — optional on Kaggle (no internet); install for parity with prod
logfire>=0.50
