[project]
name = "capture"
version = "0.0.1"
description = "Capture module — mp4 to frames + capture.yaml. Stub in Session A."
requires-python = ">=3.11"
dependencies = [
    "shared",
    "logfire>=0.50",
]

[tool.uv.sources]
shared = { workspace = true }

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["capture"]
